//! `eden-terminal` — embedded terminal as an `eden-ui` widget.
//!
//! Stub scaffolded in Phase 0; fleshed out in Phase 5 (an `alacritty_terminal`
//! core rendered through the same vello pipeline so it inherits global motion).
